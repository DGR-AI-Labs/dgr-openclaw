# GitHub About

A pre-execution gate for OpenClaw tool calls. Checks the action against policy before it runs, fails closed. Two demo tools in this release.

Topics: `["pre-execution-gate","agent-safety","tool-gating","fail-closed","policy-enforcement"]`

This file supplies the description and topics for GitHub settings. It does not change those settings. The pre-execution gate covers only the two demo tools named in README.md.
